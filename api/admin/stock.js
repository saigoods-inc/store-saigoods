/**
 * Vercel serverless path: GET|POST /api/admin/stock
 * (Same handler as api/admin-stock.js — Node server.js also routes this path.)
 */
import handler from "../admin-stock.js";

export default handler;
